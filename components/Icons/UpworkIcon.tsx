import React from "react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";

export default function UpworkIcon(props) {
  return (
    <FontAwesomeIcon icon={["fab", "upwork"]} className={props.className} />
  );
}
